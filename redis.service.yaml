apiVersion: v1
kind: Service
metadeta: 
  name: redis
  labels: 
    name: redis-service
    app: demo-voting-app
spec:
  Ports: 
  - port: 6379
    targetPort: 6379
  selector: 
    name: redis-pod
    app: demo-voting-app
      