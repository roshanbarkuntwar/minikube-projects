apiVersion: v1
kind: Service
metadeta: 
  name: db
  labels: 
    name: db-service
    app: demo-voting-app
spec:
  Ports: 
  - port: 5432
    targetPort: 5432
  selector: 
    name: postgres-pod 
    app: demo-voting-app
      